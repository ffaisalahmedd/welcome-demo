---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "welcome-test-service"
  namespace: "default"
  labels:
    app: "welcome-test"
spec:
  ports:
  - protocol: "TCP"
    port: 80
  selector:
    app: "welcome-test"
  type: "LoadBalancer"
  loadBalancerIP: ""
